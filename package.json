{
  "name": "flock-together-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Digital rules engine for Flock Together",
  "imports": {
    "#data/*": "./data/*"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test engine/dist/test/*.test.js",
    "vendor-libs": "node scripts/vendor_ui_libs.js",
    "dev": "npm run build && node scripts/serve.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "htm": "^3.1.1",
    "preact": "^10.29.8",
    "typescript": "^5.6.0"
  }
}
